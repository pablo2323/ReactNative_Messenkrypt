# MessEnkrypt React Native App

Requirements:
   - docker
   - docker-compose
   - make


Launch:
	```make```
	
Logs:

	```make logs```

Stop:
	```make stop```

Delete containers:
	```make clean```

Install docker:
	```make install_dc```

Install docker-compose:
	```m̀ake install_dcc```

